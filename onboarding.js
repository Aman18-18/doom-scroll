/**
 * onboarding.js
 * ------------------------------------------------------------------
 * Just closes this tab when the user dismisses the welcome screen.
 * ------------------------------------------------------------------
 */
document.getElementById("close-button").addEventListener("click", () => {
  window.close();
});
